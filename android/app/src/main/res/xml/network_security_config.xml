<?xml version="1.0" encoding="utf-8"?>
<!-- Cleartext is permitted ONLY for the local dev backend (emulator -> host
     loopback 10.0.2.2 and localhost), so the daily sync can reach a BE-C1
     instance running on the developer machine. All other traffic stays
     HTTPS-only, per the SPIKE finding (targetSdk 36 blocks cleartext by
     default; prod deployments must use a real HTTPS endpoint). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
